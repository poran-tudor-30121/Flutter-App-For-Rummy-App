// game_configurations_screen.dart
import 'package:flutter/material.dart';

class GameConfigurationsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Game Configurations'),
      ),
      body: Center(
        child: Text('Game Configurations Screen'),
      ),
    );
  }
}
